services:
  app:
    build:
      context: .
    container_name: agentic-url-shortener
    ports:
      - "8080:8080"
    environment:
      HOST: 0.0.0.0
      PORT: 8080
      DATABASE: shortener.db
    restart: unless-stopped
